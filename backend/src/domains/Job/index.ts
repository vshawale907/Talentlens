/**
 * Job Domain - barrel export
 * Encapsulates all types, models and services related to Job Listings
 */
export { JobModel, IJob } from '../../models/Job.model';
export { jobService } from '../../services/job.service';
